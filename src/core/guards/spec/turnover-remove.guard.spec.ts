import { TurnoverRemoveGuard } from '../turnover-remove.guard';

describe('TurnoverRemoveGuard', () => {
  it('should be defined', () => {
    expect(new TurnoverRemoveGuard()).toBeDefined();
  });
});
